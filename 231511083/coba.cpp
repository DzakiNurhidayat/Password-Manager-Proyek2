#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <ctype.h> 

using namespace std;

struct listPassword {
    string nama;
    string username;
    string password;
    string note;
};

int lowercaseString (listPassword *list){
    listPassword lower;
    int i, count;
    for (i = 0; i < count; i++)
    {
        for 
    }
    
}